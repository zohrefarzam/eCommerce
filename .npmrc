# Hoist Next + React to the workspace root so binaries and Turbopack resolve reliably
# (default isolated layout can leave broken symlinks if install is interrupted).
public-hoist-pattern[]=*next*
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*react-dom*
